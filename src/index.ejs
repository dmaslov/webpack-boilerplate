<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
    <meta charset="utf-8">
    <base href="<%= htmlWebpackPlugin.options.base %>">

  <% if (htmlWebpackPlugin.files.favicon) { %>
    <link rel="shortcut icon" href="<%= htmlWebpackPlugin.options.getAssets(htmlWebpackPlugin.files.favicon) %>" />
  <% } %>

    <title>Title</title>

  <% for (var css in htmlWebpackPlugin.files.css) { %>
    <link href="<%= htmlWebpackPlugin.options.getAssets(htmlWebpackPlugin.files.css[css]) %>" rel="stylesheet">
  <% } %>
  </head>
  <body>
    <div class="main">
      <p>
        Hello
      </p>
    </div>
  <% for (var chunk in htmlWebpackPlugin.files.chunks) { %>
    <script src="<%= htmlWebpackPlugin.options.getAssets(htmlWebpackPlugin.files.chunks[chunk].entry) %>"></script>
  <% } %>
  </body>
</html>
